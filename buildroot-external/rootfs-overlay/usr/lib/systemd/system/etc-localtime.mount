[Unit]
Description=Localtime persistent configuration
Requires=mnt-overlay.mount
After=mnt-overlay.mount hassos-overlay.service
Before=network.target

[Mount]
What=/mnt/overlay/etc/localtime
Where=/etc/localtime
Type=none
Options=bind

[Install]
WantedBy=hassos-bind.target
